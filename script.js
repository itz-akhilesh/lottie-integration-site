// script.js — for JavaScript API method
lottie.loadAnimation({
  container: document.getElementById("lottie-animation"),
  renderer: "svg",
  loop: true,
  autoplay: true,
  path: "assets/animation.json", // Local JSON file or URL
});
